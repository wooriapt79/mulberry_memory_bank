name: Mulberry Code Hygiene â RyuWon

# ì ì±:
#   scripts/ (Lynn íì´íë¼ì¸) â threshold 80, ì¤í¨ ì ì°¨ë¨
#   ê·¸ ì¸ (marrf/, src/, experiments/) â threshold 60, ê²½ê³ ë§ (ì°¨ë¨ ìì)
#   ì´ì : ì°êµ¬Â·ì¤í ìì´ì í¸ë ìì´ëì´ íë¦ì´ ë¨¼ì ë¤

on:
  push:
    branches: [main, develop]
    paths-ignore:
      - '**.md'
      - 'agent_activity.md'
      - 'training_logs/**'
      - 'daily_hunts/**'
  pull_request:
    branches: [main, develop]
    paths-ignore:
      - '**.md'
      - 'agent_activity.md'

jobs:
  # ââ Lynn íì´íë¼ì¸ (ìê²© ê²ì´í¸) ââââââââââââââââââââââââââ
  lynn-hygiene-gate:
    name: Lynn Pipeline Gate (threshold 80)
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: '3.10'
          cache: 'pip'
      - name: Install Dependencies
        run: pip install ruff pytest
      - name: Lynn Scripts Hygiene Check
        run: |
          echo "[RyuWon] Lynn íì´íë¼ì¸ ê²ì (ê¸°ì¤: 80ì )..."
          python -m src.devops.cli scripts/ --threshold 80 --output hygiene-lynn.txt
        env:
          PYTHONPATH: .
      - name: Upload Lynn Report
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: hygiene-lynn
          path: hygiene-lynn.txt
          retention-days: 30

  # ââ ì°êµ¬Â·ì¤í ìì´ì í¸ (ì¬ì  ê²ì´í¸ â ê²½ê³ ë§) ââââââââââââââ
  research-hygiene-advisory:
    name: Research Agents Advisory (threshold 60, warn only)
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: '3.10'
          cache: 'pip'
      - name: Install Dependencies
        run: pip install ruff pytest
      - name: Research Code Advisory Check
        run: |
          echo "[RyuWon] ì°êµ¬ ìì´ì í¸ ê²ì (ê¸°ì¤: 60ì , ì°¨ë¨ ìì)..."
          python -m src.devops.cli marrf/ --threshold 60 --output hygiene-research.txt || true
          echo "[RyuWon] ì°¸ê³ ì© ë¦¬í¬í¸ìëë¤. ì°êµ¬ì íë¦ì ë§ì§ ììµëë¤."
        env:
          PYTHONPATH: .
      - name: Upload Research Report
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: hygiene-research-advisory
          path: hygiene-research.txt
          retention-days: 30
